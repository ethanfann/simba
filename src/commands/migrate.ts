import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "migrate",
    description: "Migrate skills from one agent to another",
  },
  run() {
    console.log("migrate: not implemented")
  },
})
