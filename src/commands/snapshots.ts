import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "snapshots",
    description: "List and manage snapshots",
  },
  run() {
    console.log("snapshots: not implemented")
  },
})
