import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "detect",
    description: "Detect installed AI coding agents",
  },
  run() {
    console.log("detect: not implemented")
  },
})
