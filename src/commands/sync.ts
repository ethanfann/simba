import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "sync",
    description: "Sync skills between agents",
  },
  run() {
    console.log("sync: not implemented")
  },
})
