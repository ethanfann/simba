import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "backup",
    description: "Backup skills to a snapshot",
  },
  run() {
    console.log("backup: not implemented")
  },
})
