import { defineCommand } from "citty"

export default defineCommand({
  meta: {
    name: "status",
    description: "Show status of skills across agents",
  },
  run() {
    console.log("status: not implemented")
  },
})
